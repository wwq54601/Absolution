// frontend/src/hooks/useUncleNotifications.js
// Uncle Claude family event notifications.
// Events are already handled by UnifiedProgressContext's shared socket connection.
// This hook is kept as a no-op for backward compatibility.

export default function useUncleNotifications() {
  // Notifications are now handled by UnifiedProgressContext to avoid duplicate socket connections.
}
